# Railway Configuration for Code Finder Pro
# This tells Railway how to serve your static site

[build]
  builder = "NIXPACKS"

[deploy]
  startCommand = "python -m http.server 8080"
  healthcheckPath = "/"
  healthcheckTimeout = 100
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
